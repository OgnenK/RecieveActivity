var exec = require('cordova/exec');



var RecieveActivity = {
	recieveIntent : function(appName, success, error) {
		eec(success, error, "RecieveActivity", "recieveIntent", [appName];}
;
module.export = RecieveActivity;